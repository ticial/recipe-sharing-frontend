import SidebarButton from "shared/UI/sidebar-button/SidebarButton";

type Props = {};

export default function ShoppingListSidebar(props: Props) {
    return (
        <>
            <SidebarButton>All ingredients</SidebarButton>
            {/* wantToCook.map(recipe => ...) */}
        </>
    );
}
